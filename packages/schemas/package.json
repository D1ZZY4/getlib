{
  "name": "@getlib/schemas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GetLib shared Zod contracts. Single source of truth for public API/MCP validation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "biome check src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  }
}
